# --- Rust ---
/target
**/target/
*.rs.bk
*.pdb

# --- .NET ---
bin/
obj/
[Dd]ebug/
[Rr]elease/
TestResults/
*.user
*.suo
*.nupkg
project.lock.json

# --- btcpay-rs build output ---
# Packaged plugins (.btcpay, metadata sidecar, SHA256SUMS): rebuilt by dotnet/pack-plugin.sh
/artifacts

# A checkout of btcpayserver, needed because BTCPayServer.Abstractions is not published on
# NuGet. Point builds at your own copy with -p:BtcpayServerProject=...
/submodules

# uniffi bindings, produced by dotnet/regen-bindings.sh. Not committed: only people building
# this repo need them, and they have the Rust toolchain already. Plugin authors consume
# BtcpayRs.Host as a compiled assembly with the bindings inside it.
dotnet/BtcpayRs.Host/Generated/

# --- editors ---
.vs/
.vscode/
.idea/
*.swp
*~

# --- local scratch ---
/spike

# local BTCPay regtest state
/dev/plugins
